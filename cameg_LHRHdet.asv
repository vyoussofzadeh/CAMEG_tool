function [LH, RH] = cameg_LHRHdet(ssROI)

for i = 1:size(ssROI,1)
    exist 'ssROI.Var2';
    isfield(ssROI,'ROI')
    ismember(ssROI.Label,who)
    
    if ans == 1   
    R = ssROI.Var2(i);
    elseif exist(ssROI.Label); 
    if R{1}(1) == 'L'
        m(i) = 4;
    else
        m(i) = 3;
    end
end

% conns at left and right hemisphres
LH = find(m == 4);
RH = find(m == 3);

end